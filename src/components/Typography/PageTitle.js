import React from 'react'

function PageTitle({ children }) {
  return (
    <span className="my-6 text-2xl font-semibold text-gray-700 dark:text-gray-200">{children}</span>
  )
}

export default PageTitle
